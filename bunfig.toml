[install]
# Workspace installs use Bun’s local virtual store and isolated linker.
# The global store is opt-in; Docker uses hoisted installs for portable layers.
# Isolated mode retains Bun’s transitive fallback unless hoist=false is set.
linker = "isolated"
