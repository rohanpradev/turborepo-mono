{
  "name": "@repo/product-db",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:generate": "prisma contract emit",
    "db:validate": "prisma contract emit && prisma migration check",
    "db:migrate": "prisma contract emit && prisma db update --advance-ref db",
    "db:plan": "prisma contract emit && prisma migration plan",
    "db:deploy": "prisma db migrate",
    "db:verify": "prisma db verify",
    "db:adopt": "prisma contract emit && prisma db verify --schema-only --strict && prisma db sign"
  },
  "dependencies": {
    "@prisma/orm-postgres": "catalog:",
    "pg": "catalog:",
    "prisma": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/pg": "catalog:",
    "@repo/typescript-config": "workspace:*"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
