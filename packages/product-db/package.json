{
  "name": "@repo/product-db",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:generate": "bunx --bun prisma generate",
    "db:migrate": "bunx --bun prisma migrate dev",
    "db:deploy": "bunx --bun prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.9.0-dev.4",
    "@prisma/client": "^7.9.0-dev.4",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "prisma": "^7.9.0-dev.4"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
