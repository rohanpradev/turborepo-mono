# Product database environment

# Direct PostgreSQL over TCP is the default runtime path for this package.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/product_db?schema=public"

# Optional Prisma Accelerate example for managed deployments:
# DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"
